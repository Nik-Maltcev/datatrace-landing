{
  "name": "datatrace-monorepo",
  "version": "1.0.0",
  "description": "DataTrace - система поиска утечек данных и анализа компаний",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/api && npm run dev",
    "start": "cd apps/api && npm start",
    "test": "cd apps/api && npm test",
    "test:coverage": "cd apps/api && npm run test:coverage",
    "install:api": "cd apps/api && npm install",
    "build": "echo 'No build step required for this project'",
    "deploy": "echo 'Deploy using Railway or your preferred platform'"
  },
  "keywords": [
    "data-leak",
    "security",
    "company-analysis",
    "password-check",
    "monorepo"
  ],
  "author": "DataTrace Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}